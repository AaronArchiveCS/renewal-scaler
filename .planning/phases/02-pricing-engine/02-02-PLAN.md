---
phase: 02-pricing-engine
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - "~/.claude/commands/scale.md"
  - ".claude/commands/scale.md"
autonomous: false
requirements: [PRICE-03, PRICE-04]

must_haves:
  truths:
    - "The tool generates exactly 2 renewal options: one annual commitment and one 2-year commitment"
    - "Both options use the same optimal plan + add-on combination from Step 7, just different discount tiers"
    - "Annual option applies 10% discount to both base plan and add-on packs"
    - "2-year option applies 20% discount to both base plan and add-on packs"
    - "Each option shows: base plan name, base plan price, number of UGC add-on packs, add-on price, total monthly equivalent, total contract price, savings vs current spend"
    - "Savings calculation compares recommended annual spend against the customer's current annual spend from Stripe"
    - "If the customer is already on the optimal plan at the optimal price, the tool says so instead of generating redundant options"
  artifacts:
    - path: "~/.claude/commands/scale.md"
      provides: "Step 8 option generation with discount application and savings calculation"
      contains: "Step 8"
    - path: ".claude/commands/scale.md"
      provides: "Repo-tracked copy with Step 8"
      contains: "Step 8"
  key_links:
    - from: "Step 8 (option generation)"
      to: "Step 7 (optimal combo result)"
      via: "uses the optimal plan + packs count as the base for both options"
    - from: "Step 8 (savings calculation)"
      to: "Step 5 (current annual price from Stripe)"
      via: "compares recommended price against current_annual_price"
    - from: "Step 8 (discount application)"
      to: "Step 6 (pricing table discount rates)"
      via: "applies 10% annual and 20% 2-year rates from the embedded table"
---

<objective>
Add renewal option generation (Step 8) to the /scale skill file. Takes the optimal plan + add-on combination from Step 7 and produces 2 renewal options with correct discounts and savings calculations.

Purpose: This is the final pricing engine step. After this, the CSM sees actionable renewal options with real dollar amounts they can use in renewal conversations.

Output: Step 8 added to ~/.claude/commands/scale.md -- produces 2 formatted renewal options (annual and 2-year) with per-line pricing, discounts, and savings vs current spend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pricing-engine/02-CONTEXT.md
@.planning/phases/02-pricing-engine/02-01-SUMMARY.md
@~/.claude/commands/scale.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add option generation step to scale.md</name>
  <files>~/.claude/commands/scale.md, .claude/commands/scale.md</files>
  <action>
Add Step 8 to the skill file AFTER Step 7. This step generates the two renewal options.

**Step 8: Generate Renewal Options**

Instruct Claude to use the optimal combo from Step 7 and the customer's current pricing from Step 5 to produce two options.

Per D-08, generate exactly 2 options:

**Option 1: Annual Commitment (10% off)**

Per D-04 and D-06, apply 10% discount to BOTH the base plan monthly rate AND each add-on pack monthly rate:
- Base plan discounted monthly: `plan_monthly * 0.90`
- Each add-on pack discounted monthly: `$250 * 0.90 = $225`
- Total monthly equivalent: `base_discounted + (packs * $225)`
- Annual contract price: `total_monthly_equivalent * 12`

**Option 2: 2-Year Commitment (20% off)**

Per D-05 and D-06, apply 20% discount to BOTH the base plan monthly rate AND each add-on pack monthly rate:
- Base plan discounted monthly: `plan_monthly * 0.80`
- Each add-on pack discounted monthly: `$250 * 0.80 = $200`
- Total monthly equivalent: `base_discounted + (packs * $200)`
- Annual contract price: `total_monthly_equivalent * 12`
- Total 2-year contract price: `annual_price * 2`

**Savings calculation (per D-15):**

For each option, calculate:
- Customer's current annual spend: `current_monthly_price * 12` (or `current_annual_price` if the Stripe data from Step 5 already has an annual figure)
- Recommended annual spend: the annual contract price from the option
- Annual savings: `current_annual - recommended_annual`
- If savings is negative (recommended costs MORE than current), show the increase amount and note it. Do not hide cost increases.

**Per D-09, each option must display all of these fields:**
1. Base plan name (e.g., "Growth")
2. Base plan price (discounted monthly rate)
3. Number of UGC add-on packs (e.g., "2 packs")
4. Add-on price (discounted monthly rate per pack, times number of packs)
5. Total monthly equivalent
6. Total annual / total contract price
7. Savings vs current spend (or cost increase if applicable)

**Output format -- present both options as structured blocks:**

```
=== RENEWAL OPTIONS ===

Current spend: $[current_monthly]/mo ($[current_annual]/yr) on [current_plan_name]
Recommended plan: [optimal_plan_name] + [N] UGC add-on pack(s)

--- Option 1: Annual Commitment ---
Base plan:       [Plan Name] @ $[discounted_monthly]/mo (10% off $[list_monthly])
UGC add-ons:     [N] pack(s) @ $[discounted_addon_monthly]/mo each (10% off $250)
Monthly total:   $[total_monthly]
Annual price:    $[annual_total]
vs Current:      $[savings]/yr [savings | increase]

--- Option 2: 2-Year Commitment ---
Base plan:       [Plan Name] @ $[discounted_monthly]/mo (20% off $[list_monthly])
UGC add-ons:     [N] pack(s) @ $[discounted_addon_monthly]/mo each (20% off $250)
Monthly total:   $[total_monthly]
Annual price:    $[annual_total]
2-Year total:    $[two_year_total]
vs Current:      $[savings]/yr [savings | increase]
```

**Edge case -- already optimal (Claude's Discretion from CONTEXT.md):**

If the customer's current plan and price ALREADY match the optimal combo at the annual rate (within $50/mo tolerance to account for rounding), show:

```
This customer is already on the optimal plan ([Plan Name]) at a competitive rate.
Current: $[current]/mo | Optimal annual: $[optimal_annual_monthly]/mo
No plan change recommended. Consider a 2-year lock-in for additional savings.
```

Then show ONLY Option 2 (the 2-year commitment) since the annual option would be redundant.

**Per D-07: No volume discounts, no loyalty discounts, no custom pricing.** The skill file must not invent additional discount logic.

After writing to .claude/commands/scale.md (repo copy), also copy to ~/.claude/commands/scale.md for the live skill.

Also update the last line of Step 5 output. Replace:
> Data collection complete. Phase 2 will generate renewal pricing options.

With:
> Data collection complete. Calculating optimal pricing...

This makes the transition from Step 5 to Step 6-8 seamless during a live run.
  </action>
  <verify>
    <automated>grep -c "Step 8" ~/.claude/commands/scale.md && grep -c "RENEWAL OPTIONS" ~/.claude/commands/scale.md && grep -c "Annual Commitment" ~/.claude/commands/scale.md && grep -c "2-Year Commitment" ~/.claude/commands/scale.md && grep -c "vs Current" ~/.claude/commands/scale.md && echo "All checks passed"</automated>
  </verify>
  <done>
- scale.md contains Step 8 with option generation logic
- Two options generated: annual (10% off) and 2-year (20% off)
- Discounts applied to both base plan AND add-on packs per D-06
- Each option shows all 7 fields from D-09
- Savings calculation compares against current Stripe spend per D-15
- Already-optimal edge case handled gracefully
- Step 5 transition text updated for seamless flow
- Both file copies updated (repo + personal path)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete pricing engine: Steps 6-8 now embedded in the /scale skill file. The tool fetches customer data (Steps 1-5), finds the cheapest plan combo (Steps 6-7), and generates 2 renewal options with discounts and savings (Step 8).</what-built>
  <how-to-verify>
    Run `/scale` against a real customer end-to-end. After the CUSTOMER DATA PROFILE, you should now see RENEWAL OPTIONS.

    Verify the math manually for one customer:
    1. Check the optimal combo: Is it actually the cheapest? (e.g., for a ~1,200 UGC customer, Startup + 2 packs at $1,000/mo should beat Growth at $1,500/mo)
    2. Check Option 1 discount: Is the annual rate exactly 10% off the monthly? (e.g., $500/mo base becomes $450/mo)
    3. Check Option 2 discount: Is the 2-year rate exactly 20% off the monthly? (e.g., $500/mo base becomes $400/mo)
    4. Check add-on discounts: Are packs discounted too? ($250 becomes $225 annual, $200 for 2-year)
    5. Check savings: Does "vs Current" correctly compare against what Stripe says the customer pays now?

    If possible, also test with a second customer at a different usage level to confirm the optimization works across tiers.
  </how-to-verify>
  <resume-signal>Type "approved" if both options are correct with right discounts and savings, or describe what is wrong (wrong discount, bad savings math, formatting issues, etc.)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Current spend accuracy | Savings depend on Stripe price being the actual current rate -- legacy or custom pricing may not match |
| Discount rate correctness | 10% and 20% are hardcoded -- if Archive changes discount structure, skill file must be updated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | Renewal pricing in terminal | accept | Internal CSM tool, terminal output only, no persistence. Same risk profile as Step 5 data. |
| T-02-04 | Tampering | Stale discount rates | accept | Discount rates embedded in skill file, same update pattern as pricing table. Note in Step 6 reminds to update. |
| T-02-05 | Repudiation | Savings claim accuracy | mitigate | Show both current and recommended prices transparently so CSM can verify. Do not hide cost increases -- show them explicitly. |
</threat_model>

<verification>
- Step 8 generates exactly 2 options per D-08 (not 3 as in the original ROADMAP -- D-08 overrides)
- 10% annual discount applied to base + add-ons per D-04, D-06
- 20% 2-year discount applied to base + add-ons per D-05, D-06
- All 7 display fields present per D-09
- Savings compares recommended vs current per D-15
- No volume/loyalty/custom discounts per D-07
- Already-optimal edge case handled
</verification>

<success_criteria>
Running `/scale [customer]` produces a complete end-to-end output: customer data profile followed by 2 renewal options with correct discounts, total prices, and savings vs current spend. CSM verifies the math is correct against a real customer.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pricing-engine/02-02-SUMMARY.md`
</output>
